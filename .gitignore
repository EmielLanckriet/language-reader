node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Reader data. Nothing here should ever be committed: it is the reader's own
# earned data, and in this project it normally lives in the browser's OPFS
# rather than on disk at all.
*.db
*.sqlite3

# Reference data fetched at run time by the segmentation comparison harness (ADR-0012).
# Dictionaries are megabytes and are never committed; the harness downloads what it needs.
/scripts/compare-segmenters/data/
